services:
  - type: web
    name: treeguard
    env: node
    plan: free
    buildCommand: npm install --omit=optional
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
